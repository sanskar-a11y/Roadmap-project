# Automate 60

A gamified 60-day AI Automation Specialist roadmap built with React and Vite.

## Run locally

```bash
npm install
npm run dev
```

Progress, XP, streaks, and achievements are saved in the browser with `localStorage`.
